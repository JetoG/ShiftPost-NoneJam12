// Limpando a Particula
part_type_clear(pt_normal)
part_system_clear(ps_normal);

// Destruindo a Particula
part_type_destroy(pt_normal);
part_system_destroy(ps_normal);

// Limpando a Particula
part_type_clear(pt_shadow)
part_system_clear(ps_shadow);

// Destruindo a Particula
part_type_destroy(pt_shadow);
part_system_destroy(ps_shadow);